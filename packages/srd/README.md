# @dm-forge/srd

Versioned SRDs (dnd5e, pf2e, ...). Placeholder until the first SRD-search feature.

Loaded via `pnpm db:seed:srd <system>` (script will be added with the feature). See `docs/architecture-overview.md` → SRD search for the FTS approach (no embeddings, no vector store).
