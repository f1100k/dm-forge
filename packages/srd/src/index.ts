// Pacote placeholder para SRDs versionadas (dnd5e, pf2e, ...).
// Conteúdo será carregado via `pnpm db:seed:srd <system>` quando a primeira
// feature de busca em SRD chegar (ver docs/architecture-overview.md → SRD search).
export const SRD_PLACEHOLDER = true
