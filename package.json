{
  "name": "dm-forge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "test": "vitest run",
    "test:watch": "vitest",
    "gen:secrets": "node scripts/gen-secrets.mjs",
    "db:migrate": "node scripts/with-env.mjs pnpm --filter @dm-forge/db exec prisma migrate",
    "db:studio": "node scripts/with-env.mjs pnpm --filter @dm-forge/db exec prisma studio",
    "db:seed": "node scripts/with-env.mjs tsx packages/db/src/seed.ts",
    "db:reset": "node scripts/with-env.mjs pnpm --filter @dm-forge/db exec prisma migrate reset",
    "db:generate": "node scripts/with-env.mjs pnpm --filter @dm-forge/db exec prisma generate",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.10.5",
    "dotenv": "^16.4.7",
    "dotenv-expand": "^12.0.1",
    "prisma": "^6.1.0",
    "tsx": "^4.19.2",
    "turbo": "^2.3.4",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4",
    "zod-prisma-types": "^3.2.4"
  }
}
